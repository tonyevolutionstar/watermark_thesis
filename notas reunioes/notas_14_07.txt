quantos documentos sao impressos por ano - pergunta proxima semana
sequencial

union join
n_versao do qrcode*, 0 a 256, 
outra base de dados para a criação do qrcode 
que vai ter timestamp, posição do qrcode no pdf. aceite ou reprovado.

mostrar o pdf gerado, butao sim ou nao 
guardar as tentativas.


*com base no numero de versao tomar decisoes de leitura - ultima coisa a fazer, talvez

numero máximo de tentativas para gerar o qrcode. se atingir dá erro.
antes de mostrar ao utilizador verificar se foi lido. 
gera logs cada vez que gera um qrcode